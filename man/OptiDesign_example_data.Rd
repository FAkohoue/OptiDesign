% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_example.R
\docType{data}
\name{OptiDesign_example_data}
\alias{OptiDesign_example_data}
\title{Example data for OptiDesign}
\format{
A named list with the following components:
\describe{
\item{\code{OptiDesign_lines}}{A data frame with columns \code{Treatment} and \code{Family}
representing a synthetic pool of line identifiers and their family labels.}

\item{\code{OptiDesign_id_map}}{A data frame with columns \code{Treatment} and \code{LineID}
used to map treatment labels to row and column names of relationship matrices
when those names differ.}

\item{\code{OptiDesign_GRM}}{A synthetic genomic relationship matrix with rownames
and colnames corresponding to line IDs. It can be used for matrix-based
grouping or genomic dispersion examples.}

\item{\code{OptiDesign_A}}{A synthetic pedigree-style relationship matrix with
rownames and colnames corresponding to line IDs. It can be used for
pedigree-based grouping or dispersion examples.}

\item{\code{OptiDesign_K}}{A synthetic relationship or kernel matrix with rownames
and colnames corresponding to line IDs. It is intended for workflows that
require a \code{K} matrix, such as BLUP-style prediction settings or dispersion
optimization using \code{dispersion_source = "K"}.}

\item{\code{OptiDesign_famoptg_example}}{A named list containing the core treatment
vectors and field dimensions needed to run \code{prep_famoptg()}. This includes
checks, p-rep treatments, unreplicated treatments, and basic field dimensions.}

\item{\code{OptiDesign_alpha_example}}{A named list containing the core treatment
vectors and field dimensions needed to run \code{alpha_rc_stream()}.
This includes checks, entries, number of replicates, and fixed field dimensions.}

\item{\code{OptiDesign_famoptg_args_family}}{A named list of additional arguments
for a family-based call to \code{prep_famoptg()}. It illustrates a simple design
setup without efficiency evaluation.}

\item{\code{OptiDesign_famoptg_args_grm}}{A named list of additional arguments for
a GRM-based call to \code{prep_famoptg()}. It illustrates matrix-based grouping and
optional dispersion settings.}

\item{\code{OptiDesign_alpha_args_family}}{A named list of additional arguments
for a family-based call to \code{alpha_rc_stream()}. It illustrates
a stream-based row-column design without efficiency evaluation.}

\item{\code{OptiDesign_alpha_args_grm}}{A named list of additional arguments for
a GRM-based call to \code{alpha_rc_stream()}. It illustrates
matrix-based grouping and optional dispersion settings.}
}
}
\source{
Generated internally by \code{data-raw/generate_example_data.R}.
}
\description{
Synthetic example data shipped with \code{OptiDesign} for illustrating and testing
the main design constructors, relationship-matrix workflows, optional
dispersion optimization, and optional efficiency settings.
}
\details{
The objects in this dataset are synthetic and are intended for:
\itemize{
\item package examples,
\item tests,
\item workflow demonstrations,
\item and user exploration of argument combinations.
}

The dataset is especially useful for understanding how the package handles:
\itemize{
\item repeated checks,
\item p-rep and unreplicated materials,
\item fixed-grid stream designs,
\item family-based grouping,
\item GRM- or pedigree-based grouping,
\item and matrix-based dispersion settings.
}

Load the dataset with:

\code{data("OptiDesign_example_data", package = "OptiDesign")}

Then extract individual components as needed, for example:

\code{x <- OptiDesign_example_data}

\code{x$OptiDesign_famoptg_example}

\code{x$OptiDesign_GRM}

\code{x$OptiDesign_alpha_args_family}
}
\keyword{datasets}
